# cpppkg v1.1.0

## Release notes

The install folder for packages has been changed from `.cpppkg` to `.cpppkg/packages` for easier access to generated CMake files.

> [!IMPORTANT]
> This changes requires to run `cpppkg i` to patch `.cpppkg/Declare.cmake` and `.cpppkg/Link.cmake`
